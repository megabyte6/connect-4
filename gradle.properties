# Set the version of the app
appVersion = 2.1

# Set the version of JavaFX to use
javafxVersion = 25.0.3

# Specify the output format for jpackage
# Options: msi, exe, deb, rpm, dmg, pkg, default
jpackageFormat = default
